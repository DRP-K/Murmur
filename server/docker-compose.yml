services:
  relay:
    image: ghcr.io/drp-k/social-relay:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - relay-data:/data
    environment:
      PORT: "8080"
      DATA_DIR: "/data"

volumes:
  relay-data:
